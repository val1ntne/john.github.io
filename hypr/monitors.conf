# Monitors — safe auto layout
# Tip: replace with specific outputs as needed (use `hyprctl monitors`)

monitor = , preferred, auto, 1

# Example laptop + external setup (commented)
# monitor = eDP-1, 1920x1200@60, 0x0, 1
# monitor = DP-1, 2560x1440@144, 1920x0, 1
